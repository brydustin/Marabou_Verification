# Formalization map

Revision scope and source paths are as in [MARABOU_SOURCE_MAP.md](MARABOU_SOURCE_MAP.md).
Paths in the table are relative to `upstream/Marabou/`. These are documented
correspondences, **not refinement theorems about the C++ program**.

| HOL concept | Source counterpart | Chosen abstraction / mismatch |
| --- | --- | --- |
| `var = nat` | `Equation::Addend::_variable`, `InputQuery::getNumberOfVariables/getNewVariable`; `src/engine/Equation.h`, `InputQuery.{h,cpp}` | Unbounded natural-number names. C++ uses finite-width `unsigned` and a declared variable count. An importer must check indices. |
| `valuation = var ⇒ real` | `Tableau::_basicAssignment/_nonBasicAssignment`, `getValue`; `InputQuery::_solution`, `getSolutionValue`; `src/engine/Tableau.{h,cpp}`, `InputQuery.{h,cpp}` | Total exact real function. C++ holds finite `double` arrays/maps and may lack an assignment. |
| `Linexpr c ts` | `src/common/LinearExpression.{h,cpp}` (`_constant`, `_addends`, `evaluate`); `Equation::Addend` | Finite list of real coefficients with duplicates allowed. `LinearExpression` has a map; `Equation` has a list. No C++ container invariant is assumed. |
| `eval_linexpr` | `LinearExpression::evaluate` | Exact sum of **all** terms. C++ skips coefficients considered zero by `FloatUtils`, uses floating-point addition/multiplication, and can return NaN for missing assignments. No equality with that implementation is claimed. |
| `LinearEq`, `LinearLe`, `LinearGe` | `src/engine/Equation.{h,cpp}`, `Equation::EQ/LE/GE` | Direct exact comparison with the right-hand scalar. Marabou's Equation has no separate left constant; `c + sum ts op b` corresponds algebraically to `sum ts op (b-c)`. That arithmetic translation is not an implemented importer. |
| `Lower x l`, `Upper x u` | `src/engine/Tightening.h`, `Tightening::LB/UB`; `InputQuery`, `Query`, `BoundManager` | Declarative propositions in a conjunction. C++ setters overwrite, tightening methods select stronger bounds, and propagation mutates state. These operations are not modeled. |
| No bound atom | `InputQuery::getLowerBound/getUpperBound`, `Preprocessor::setMissingBoundsToInfinity` | Unrestricted direction, instead of a floating-point infinity. NaN and infinity are never HOL real values. |
| `ReLU x y` / `satisfies_relu` | `src/engine/ReluConstraint.{h,cpp}` (`_b`, `_f`, `satisfied`) | `x` is backward/input `b`; `y` is forward/output `f`. Exact `v y = max 0 (v x)` is the intended relation, while `satisfied()` uses tolerances. |
| `query` | `src/engine/IQuery.h`, `InputQuery.{h,cpp}`, `Query.{h,cpp}` | Three finite conjunctions: linear atoms, bounds, ReLUs. Omits variable count, input/output markers, ownership, contexts, non-ReLU constraints, network metadata and solver state. |
| `tableau_state`, `tableau_candidate`, `tableau_rows_satisfied` | `src/engine/Tableau.{h,cpp}`: `_basicVariables`, `_nonBasicIndexToVariable`, `_basicAssignment`, `_nonBasicAssignment`; `computeAssignment` | Exact solved-row abstraction with finite disjoint basic/nonbasic sets. Rows represent x_B = β − B⁻¹A_N x_N. It does not model the native matrix, factorization, indexed storage, floating-point arithmetic, variable statuses, or caches. |
| `update_nonbasic_assignment` | `Tableau::setNonBasicAssignment(variable, value, true)`, `Tableau::computeChangeColumn`; `src/engine/Tableau.cpp` lines 1452–1504 | For row coefficient c_bj = −(B⁻¹A_j)_b, performs x_j := r and x_b := x_b + c_bj(r − x_j). HOL proves exact row consistency, well-formedness, and that the selected nonbasic remains in bounds when its target is in bounds. It does not verify native factorization, tolerance-based basic statuses, or cache invalidation. |
| `exchange_basis`, `pivot_admissible`, `solve_row`, `substitute_row` | `Tableau::performPivot` (696–801) and `performDegeneratePivot` (803–850); rows as in `Tableau::getTableauRow` (1568–1607) | Explicit solved rows instead of a factorized basis. A nonzero exact pivot element replaces the native tolerance and row/column agreement checks. `exchange_models`, `exchange_candidate` and `exchange_rows_satisfied` are proved; `solved_rows_unique` states the matrix interpretation as an obligation on the native rows, not a proof about the factorization. |
| `status_of`, `core_cost`, `reduced_cost`, `entering_direction` | `Tableau::computeBasicStatus` (425–452); `CostFunctionManager::computeCoreCostFunction/computeBasicOOBCosts`; `Tableau::eligibleForEntry` (617–666) | Exact comparisons with zero tolerances and a freshly computed core cost. Incremental cost updates, heuristic costs and the optimizing mode are not modeled. |
| `basic_ratio`, `exact_harris_ratio_test`, `apply_choice`, `native_leaving_target` | `Tableau::harrisRatioTest` (1057–1440), `Tableau::updateAssignmentForPivot` (2345–2457) | The default ratio test with its relaxation tolerance set to zero; ratio-clamping for stale costs never arises. `exact_harris_admissible`, `choice_invariants` and `leaving_native_formulas` are proved. |
| `tableau_layout`, `layout_abstracts`, `native_pivot`, `native_degenerate_pivot`, `native_bound_flip` | `_basicIndexToVariable`, `_nonBasicIndexToVariable`, `_variableToIndex`, `_basicAssignment`, `_nonBasicAssignment` in `Tableau.h`; Tableau.cpp 779–782, 828–831, 839–841, 2375–2456 | Exact lists instead of arrays of doubles; the refinement theorems show that the array updates implement the variable-indexed transitions. |
| `simplex_run`, `no_entering_candidate_infeasible`, `simplex_run_sound` | `Engine::performSimplexStep` (Engine.cpp 632–831) and its `InfeasibleQueryException` branch (776–786) | A fuelled loop taking the first eligible nonbasic instead of the native entry strategies and retry search. `Feasible` and `Infeasible` are sound for the tableau's rows and bounds; `Out_Of_Fuel` claims nothing; termination is not proved. |
| `engine_ready`, `initial_tableau`, `lower_of`, `upper_of`, `extend_aux` | `Engine::invokePreprocessor` (980–1012), `createConstraintMatrix` (1061–1089), `addAuxiliaryVariables` (1290–1314), `Tableau::initializeTableau` (336–374), `computeAssignment` (376–411) | Equations only and finite bounds, as the engine requires. Several bound atoms are read as a conjunction where native setters keep the last; repeated variables are summed where `createConstraintMatrix` keeps the last coefficient (the file workflow rejects both). `removeRedundantEquations` is not modeled; the harness requires it to remove nothing. |
| `select_initial_basis`, `pivot_sequence`, `reset_assignment`, `native_initial_tableau` | `Engine::selectInitialVariablesForBasis` (1115–1288), `augmentInitialBasisIfNeeded` (1316–1328) | An exact copy of the triangular selection, with exact zero tests instead of `FloatUtils::isZero`; the basis is reached by checked exchanges. `Imported_Marabou_Initial_Bases` proves agreement with the native orders on 20 captured runs. |
| `solve_linear_part`, `solve_linear_part_native`, `solve_query` | The native LP path of `Engine::processInputQuery` followed by the simplex loop | `Simplex_Unsat` proves the query unsatisfiable; `Simplex_Feasible` satisfies the linear part and bounds; ReLUs are relaxed, not split. |
| `bound_store`, `set_bound`, `tighten_bound`, `comply`, `propagate_tightenings` | `BoundManager::{setLowerBound,setUpperBound,tightenLowerBound,tightenUpperBound,propagateTightenings}` (BoundManager.cpp 145–284, 315–378); `Tableau::updateVariableToComplyWith{Lower,Upper}BoundUpdate` (Tableau.cpp 1785–1827) | Exact comparisons; consistency uses exact `≤` instead of `FloatUtils::gte`. Bound explanations (`BoundExplainer`) are not modeled, and cost-function invalidation needs no counterpart because costs are recomputed. |
| `first_conflict`, `conflict_no_bounded_model`, `branch_of`, `apply_decision`, `apply_derived` | `_firstInconsistentTightening`, `allBoundsValid` in `Engine::solve` (313–317); `Engine::applySplit` (1994–2141) with `isPhaseFixing` ground bounds | Decisions are an explicit list in the branch; a bound applied as derived must be entailed (`undeclared_decision_breaks_branch`). Merging and split equations are not modeled. |
| `row_rules`, `rule_bound`, `apply_rules` | `RowBoundTightener::tightenOnSingleInvertedBasisRow` (RowBoundTightener.cpp 237–402) | Exact sign-selected row bounds without the 10⁻⁶ rounding constant or the minimal-coefficient skip; each term is bounded separately. |
| `query_vars`, `rat_query_vars` | Fresh-name requirement behind `Engine::addAuxiliaryVariables` allocating at `originalN+count` | Syntactic support over all query components, including zero coefficients. No proof of C++ variable-count or allocation invariants is assumed. |
| `introduce_inequality_aux`, `inequality_aux_model_extension/projection` | `src/engine/Preprocessor.cpp::makeAllEquationsEqualities`, lines 224–244 | Replace one indexed LE/GE atom with `e+s=b`; coefficient is +1 in both cases. LE adds `s≥0`, GE adds `s≤0`. Freshness gives real SAT/UNSAT equivalence; the native loop remains unverified. |
| `rat_introduce_inequality_aux` / `rat_introduce_inequality_aux_sequence` | Exact mathematical checking counterpart of those replacements | Read direction/expression/scalar from each current atom, check global freshness, construct the result and reject failed steps. No finite opposite slack bound is assumed. |
| `check_after_inequality_aux_sequence_sound` / `check_assignment_after_inequality_aux_sequence_sound` | Composition with the existing checked ReLU/tableau pipeline and exact SAT witnesses | Proves source-query UNSAT or a real model. Current examples are hand-written; no native inequality conversion has yet been imported. |
| `introduce_fixed_aux`, `fixed_aux_model_extension/projection` | `src/engine/Engine.cpp::addAuxiliaryVariables`; `Query::setLowerBound/setUpperBound` | One indexed equality `e=b` becomes `e-s=0` with both bounds of `s` fixed to `b`. With global freshness, SAT/UNSAT equivalence and model extension/projection are proved. The C++ loop and mutations remain unverified. |
| `rat_introduce_fixed_aux`, `check_after_fixed_aux` | An exact mathematical checking interface for that single transformation | Checks index, equality type, and global freshness; constructs the result and reuses the existing certificate checker. Embedding and source-query UNSAT soundness are proved. No new native certificate schema or decoder is asserted. |
| `Tableau_Auxiliary_Examples.solver_linear_before_aux_unsatisfiable` | Explicit rational query matching the `linear` scenario in `Isabelle/tools/solver_capture/capture.cpp` | Its checked single auxiliary introduction equals the earlier captured processed query exactly. The native certificate proves this explicit source query UNSAT. The hand-written source description is not a verified C++ input decoder. |
| `rat_introduce_fixed_aux_sequence`, `check_after_fixed_aux_sequence` | Mathematical composition of the equation transformations in `Engine::addAuxiliaryVariables` | Checks each proposed pair against the preceding result and rejects any failure. Induction proves SAT/UNSAT preservation and source-query certificate soundness. This is not a refinement proof of the native C++ loop. |
| `Tableau_Auxiliary_Sequence_Examples.solver_split_before_aux_unsatisfiable` | Explicit input from the `relu_split` scenario and its existing processed snapshot/certificate | Five checked introductions produce the snapshot exactly. A separate theorem accounts for `f-b-a` versus column order `-b+f-a`. The source query and step list are hand-written HOL data; the native certificate is reused unchanged. |
| `introduce_relu_aux`, `relu_aux_model_extension/projection` | `src/engine/ReluConstraint.cpp::transformToUseAuxVariables`, lines 936–978 | Keep the ReLU, append `f-b-a=0` and `a≥0`, optionally cap `a` by `max(0,-l)`. Models extend with `a=f-b`; freshness gives SAT/UNSAT preservation. No auxiliary metadata or C++ no-op branch is modeled. |
| `rat_introduce_relu_aux`, `embed_rat_relu_aux_query` | Exact checking counterpart for that new-variable transformation | Check ReLU membership, all-component freshness, and an explicit finite input lower bound if selected. Construct the row/bounds and prove their real embedding. `None` omits a finite cap; no real infinity is used. |
| `check_after_relu_aux_sound` | Mathematical composition with `Engine::addAuxiliaryVariables` and native proof evidence | One checked ReLU introduction, the existing finite scalar-fixed sequence, and recursive certificate acceptance imply source-query UNSAT. The native cached-bound/allocation invariants are not assumed. |
| `ReLU_Auxiliary_Examples.solver_before_relu_aux_unsatisfiable` | Existing `relu_aux` source snapshot and solver-produced proof | The new introduction from explicit four-variable HOL data gives exactly the saved five-variable source. The native capture already supplied that ReLU auxiliary; this earlier source/step was not captured from C++. |
| `Imported_Marabou_Native_Relu_Intro.imported_relu_introduction_matches_source` | `ReluConstraint.cpp::transformToUseAuxVariables` called by `capture.cpp::introduce_native_relu_aux` after `Preprocessor::informConstraintsOfInitialBounds` | Actual before/after snapshots and a harness call record are imported. HOL computes the guarded step and checks the entire independent after-query. General preprocessing is disabled. |
| `Imported_Marabou_Native_Relu_Intro.imported_before_relu_query_unsatisfiable` | New `relu_intro` scenario: one native ReLU introduction followed by engine initialization and `Engine::solve` | The explicit captured four-variable source is UNSAT over real valuations, through the ReLU step, three scalar-fixed steps and native proof. No theorem verifies the C++ extractor or JSON decoder. |
| `rat_introduce_relu_aux_sequence` / `check_after_relu_aux_sequence_sound` | Mathematical composition corresponding to successive `ReluConstraint::transformToUseAuxVariables` calls in `Preprocessor::transformConstraintsIfNeeded` | Every step checks the current query. Successful composition preserves real SAT/UNSAT and lifts tableau/proof acceptance to the original query. Native iteration and bound caches are not assumed correct. |
| `Imported_Marabou_Native_Relu_Sequence.imported_relu_introductions_match_source/imported_before_relu_query_unsatisfiable` | `capture.cpp::introduce_native_relu_aux_sequence` and the actual `relu_sequence` solver scenario | Two native calls, complete before/after snapshots, five tableau steps, two native output-upper lemmas and a linear leaf replay to UNSAT of the explicit six-variable source. |
| `Imported_Marabou_Source_Relu_Aux_Inactive.imported_source_query_unsatisfiable` | The `relu_aux_inactive` scenario in `capture.cpp`; one native `aux LB -> f UB` PLC lemma from `Engine::solve` | The captured five-variable source, whose auxiliary is an ordinary source variable with an explicit equation, is UNSAT through three tableau steps, the checked positive-auxiliary rule and a linear leaf. The native variable order puts the auxiliary first so its bound notification precedes the output's. |
| `Imported_Marabou_Native_Relu_Chain.imported_before_relu_query_unsatisfiable` | The `relu_chain` scenario and the previously rejected chained proof | Two native ReLU introductions, five tableau steps and all four native PLC lemmas replay to real UNSAT of the captured six-variable starting query. The fresh run reproduces the six historical data artifacts exactly; extraction and decoding remain unverified. |
| `ReLU_Auxiliary_Sequence_Examples.native_source_needs_both_relus` | A mathematical check on the new native before-query | Removing either ReLU admits an explicit real model, so the second constraint is substantive. These SAT witnesses are proved HOL data, not claims about native SAT runs. |
| `check_rat_assignment`, `assignment_valuation` | `src/engine/Engine.cpp::extractSolution` (lines 1736–1775) copies `Tableau::getValue` doubles into `IQuery::setSolutionValue` | Finite rational assignment, unlisted variables 0, distinct variables required; exact evaluation of every linear atom, bound and ReLU. Equivalent to being a real model of the embedded query. The native doubles are only candidate data for an untrusted reconstruction. |
| `Imported_Marabou_Native_Relu_Sat.imported_*_model` | The `relu_sat` scenario: two native ReLU introductions, engine initialization, `Engine::solve` returning SAT, then `extractSolution` on the processed query | The exact binary values of the reported doubles satisfy the explicit processed, source and before-ReLU queries. No theorem relates the C++ SAT return value or its tolerances to these predicates. |
| `decode_query`, `encode_query`, `decode_encode_query` | No Marabou counterpart; a project-defined input representation, `marabou-exact-query-v1` | The HOL decoder is the meaning of the bytes. Exact rationals, one-space tokens, LF-terminated lines; everything else rejects. Round trip proved for constant-free linear expressions. |
| `Imported_Marabou_Exact_Texts.solver_*_text_*` | Captured starting queries of the ten native scenarios, exported afterwards from their JSON snapshots | Theorems about the decoded bytes of checked files. The native harness built its `InputQuery` in C++ and did not read the files. |
| `same_constraints`, `decodes_like` | No Marabou counterpart | Constraint-set equality of rational queries; proves that results transfer between orderings and spellings of the same constraints. |
| `Imported_Marabou_Example_*.query_file_*` | `capture.cpp::read_query_file` (untrusted C++ reader) building the `InputQuery`, then the native-introduction and solve path | Theorems about the decoded bytes of files the native engine read itself. The reader is not trusted: Isabelle proves that the file's decoding has the captured constraints. |
| `satisfies_query`, `satisfiable`, `unsatisfiable`, `models` | Intended query meaning; `Engine::solve`/`IEngine::ExitCode` report operational outcomes | These define a mathematical property. No theorem equates an `ExitCode` or C++ Boolean with these predicates. |
| `active_split Q x y` | `ReluConstraint::getActiveSplit` and `Engine::applySplit` | Adds `0 ≤ x` and `y - x = 0`; removes the selected ReLU. C++ uses `b - f = 0` or an auxiliary bound and manages activation state. Both equation orientations express the same exact equality, but no C++ state-preservation theorem exists. |
| `inactive_split Q x y` | `ReluConstraint::getInactiveSplit` | Adds `x ≤ 0` and explicit `y = 0`; removes the selected ReLU. C++ adds upper bounds on `b` and `f`, relying on an existing nonnegative `f` bound. |
| `unsatisfiable_relu_split` | Branch coverage intended by `getCaseSplits`, `SearchTreeHandler::performSplit/popSplit` | A logical inference with explicit membership and two UNSAT premises. Does not verify branch selection, bookkeeping, restoration, or exhaustiveness of the running search. |
| `query_inconsistent_bounds` | `BoundManager::consistentBounds`, `Checker::checkContradiction` for an empty contradiction vector | Exact strict inconsistency `u < l`. Does not trust tolerance tests or the provenance of any propagated bound. |
| `linear_le_add`, `linear_le_scale_nonnegative` | Mathematical ingredients of `BoundExplainer` / `UNSATCertificateUtils` | Sound real arithmetic rules. The rational leaf checker now proves analogous weighted-sum reasoning, without a reconstruction proof for C++ evidence. |
| `rat_linexpr`, `rat_linear_constraint`, `rat_bound`, `rat_query` | Rational mathematical counterparts of the same expression/query concepts | Separate executable data types. The C++ types still use doubles; no conversion from them has been verified. |
| `embed_linexpr`, `embed_linear`, `embed_bound`, `embed_query` | No asserted C++ implementation counterpart | Exact `of_rat` interpretation into the original real syntax. ReLU atoms are preserved. Valuations remain real-valued. |
| `normalize_linear`, `normalize_bound`, `normalize_query` | A mathematical alternative to the transformations in `Preprocessor.cpp` | Proved normalization to expressions constrained to be nonpositive, including two rows per equality. This does not verify Marabou's preprocessing. |
| `weighted_sum`, `collect_terms`, `check_linear_leaf` | Related evidence in `src/proofs/Contradiction`, `BoundExplainer`, `UnsatCertificateUtils`, `AletheProofWriter` | Our weights refer to normalized query inequalities, not tableau rows. Exact length/sign/cancellation checks imply `unsatisfiable (embed_query Q)`. The separate JSON adapter reconstructs candidate weights from a restricted native format. |
| `rat_active_split`, `rat_inactive_split` | `ReluConstraint::getActiveSplit/getInactiveSplit` | Their embeddings are proved equal to the existing mathematical real splits. Native auxiliary bounds must be reconstructed from these canonical phase constraints and root rows. |
| `certificate`, `check_certificate` | `src/proofs/UnsatCertificateNode.{h,cpp}` | Finite tree with linear leaves, binary ReLU splits, and unary checked linear-bound/ReLU propagation. Splits check membership and both children; propagation checks its premises and continuation. Omits C++ visited/SAT/delegation flags. No unchecked success path exists. |
| `check_relu_upper_bound`, `rat_add_bound`, `Relu_Upper` | `src/engine/ReluConstraint.cpp::notifyUpperBound`, input-variable branch; `src/engine/BoundManager.cpp::addLemmaExplanationAndTightenBound`; `src/proofs/PlcLemma.{h,cpp}` | Requires a present ReLU and explicit input upper bound `u`, then adds an output upper bound `b ≥ max(0,u)`. Exact rational guards replace tolerance-based acceptance. Existing constraints are retained. Imported premises may now come from a checked linear implication. |
| `check_relu_output_aux_upper_bound`, `Relu_Output_Aux_Upper` | `src/engine/ReluConstraint.cpp::notifyLowerBound`, positive `_f` branch; `src/proofs/Checker.cpp::checkReluLemma`, lines 674–678 | A present ReLU, explicit strictly positive output lower bound, and two checked implications for `y-x-a=0` justify `a≤u` with `0≤u`. HOL uses exact strict positivity; the native checker tests the bound plus epsilon. |
| `rat_relu_output_aux_upper_preserves_models` | Semantic justification for the preceding bound update | Proves equality of real model sets after adding the accepted bound. At output zero the rule is invalid; a real counterexample and checker rejection are proved. |
| `check_relu_aux_lower_output_upper_bound`, `Relu_Aux_Lower_Output_Upper` | `src/engine/ReluConstraint.cpp::notifyLowerBound`, positive `_aux` branch, lines 207–226; `src/proofs/Checker.cpp::checkReluLemma`, lines 685–689 | A present ReLU, an explicit strictly positive AUXILIARY lower bound, and two checked implications for `y-x-a=0` justify `y≤u` with `0≤u`. The native follow-up `b≤-l` through the tightening row is a linear explained update, not part of this rule. HOL uses exact strict positivity; the native checker tests the bound plus epsilon. |
| `rat_relu_aux_lower_output_upper_preserves_models` | Semantic justification for that output bound | Proves equality of real model sets after adding the accepted bound. At auxiliary zero (`x=y=1, a=0`) the rule is invalid; a real counterexample and checker rejection are proved. |
| `relu_hull_query`, `check_relu_fixed_active/inactive`, `Relu_Fix_Active/Inactive` | `Preprocessor::informConstraintsOfInitialBounds` (1121–1142) with `ReluConstraint::checkIfLowerBoundUpdateFixesPhase/checkIfUpperBoundUpdateFixesPhase` (125–146); `Engine::solve` → `applyAllValidConstraintCaseSplits` (207–208); `Engine::applySplit` proof branch (2108–2134) | Native code fixes a phase with epsilon tests and adds the valid split's tighter bounds as unexplained ground bounds. HOL instead needs an exact linear proof of a phase-deciding bound from the query plus `y≥0`, `y≥x`, then checks the continuation on that phase's split query. `relu_fixed_*_models` proves the model sets equal. Disabling the constraint and the context bookkeeping are not modeled. |
| `fact`, `apply_facts`, `check_projection`, `check_projection_model` | Result of `Preprocessor::preprocess` (Preprocessor.cpp 61–180): `processEquations`, `processConstraints`, `processIdenticalVariables`, `collectFixedValues`, `eliminateVariables`; maps `getNewIndex`/`variableIsFixed`/`variableIsMerged` (1013–1044) | Checks the preprocessed query, not the procedure. Facts are exactly implied bounds, hull rows, phase facts and the existing ReLU rules. Every renamed row of the result must be exactly implied, and every ReLU linked to an original ReLU up to proved equalities. Native epsilon acceptance and almost-fixed snapping have no counterpart: unimplied results are rejected. |
| `check_linear_implication`, `check_linear_bound`, `Linear_Bound` | `src/proofs/UnsatCertificateUtils.cpp::computeBound/getExplanationRowCombination`; `BoundExplainer::updateBoundExplanation` | Checks that a target minus a nonnegative normalized-row combination is a nonpositive constant. Proves both upper and lower bounds over real models. The adapter reconstructs upper and lower premises from `e_x + wᵀA`; it does not verify the native floating-point computation. |
| `rat_linear_bound_preserves_models` | Semantic justification for adding a linearly explained bound | Proves equality of real model sets before/after the checked addition. The adapter keeps this HOL premise separate from native ground state; only PLC conclusions update native ground bounds. |
| `rat_relu_upper_preserves_models` | Intended semantic justification for that propagation | Proves equality of real model sets before/after the checked addition. Allows redundant additions; does not verify C++ state updates, phase fixing, or the explanation manager. |
| `relu_aux_expr`, `check_relu_aux_upper_bound`, `Relu_Aux_Upper` | `ReluConstraint::notifyLowerBound`, input-to-auxiliary branch; `transformToUseAuxVariables` supplies the equation in native preprocessing | Checks ReLU membership, an input lower bound, two exact linear witnesses for `f-b-aux=0`, and `max(0,-l)≤u`. Metadata alone never supplies the equation. New-variable introduction is verified separately in `ReLU_Auxiliary`. |
| `rat_relu_aux_upper_preserves_models` | Semantic justification for that auxiliary propagation | Adding an accepted upper bound on the proved auxiliary preserves the entire real model set. |
| `check_certificate_sound` | Logical composition intended by the search proof tree | Proved by induction from exact leaf soundness, ReLU coverage, and checked propagation; no premise concerning solver correctness or its UNSAT flag. |
| `Imported_Marabou_*.imported_query` | `src/proofs/JsonWriter.cpp::writeProofToJson` top-level tableau, bounds, constraints | The untrusted adapter decodes exact serialized decimal rationals and matches a separately supplied processed-query manifest. No theorem relates the decoded query to source bytes, binary-double values, or an original preprocessed input. |
| `Imported_Marabou_*.imported_query_unsatisfiable` | JSON-writer test fixtures produced from actual C++ `UnsatCertificateNode` / `Contradiction` objects | Kernel-checked real UNSAT for the explicit query data. Nine fixtures use hand assembly or native component calls on supplied rows. The six `Imported_Marabou_Solver_*` theories replay actual `Engine::solve` certificates, including output/auxiliary propagation in both auxiliary directions and a binary split. |
| Nine `solver_*_query.json` snapshots and their capture harness | `Engine::getQuery/storeState/getGroundBound`, before `Engine::solve` | Independently captures initialized equations/bounds and compares them with the native tableau at runtime. This observation is unverified C++ extraction, not a refinement theorem or a preprocessing proof. |
| Nine `solver_*_source.json` snapshots | `src/engine/InputQuery.cpp::generateQuery`, called by `capture.cpp::snapshot_source` before engine initialization | Copy and serialize the actual supplied query, retaining nonzero scalars and addend order. Extraction and decoding are unverified; generated HOL explicitly records the resulting source. |
| Nine `solver_*_steps.json` lists | `capture.cpp::snapshot_steps` observes new columns corresponding to `Engine::addAuxiliaryVariables` | Harness proposals recovered from the processed rows, not a native event log. Each step and exact final query equality are independently checked in HOL. |
| `Imported_Marabou_Source_*.imported_source_term_order/imported_steps_match_query` | Source addend order versus `Engine::createConstraintMatrix` and native CSR column order | Proved pointwise equivalence for the proposed permutation, followed by a checked sequence yielding the entire separately captured processed query. No unverified row normalization is assumed. |
| `Imported_Marabou_Source_*.imported_source_query_unsatisfiable` | The six source-imported native scenarios | Real-semantic UNSAT of the explicit captured source through checked introductions and native proof replay. Neither the JSON decoder nor the native initialization loop is verified. |
| `Solver_ReLU_Examples.captured_query_requires_nonlinear_evidence` | A mathematical check on the captured ReLU query | Its linear relaxation has an explicit real model. No rational linear-leaf certificate alone can certify the root; the replayed nonlinear rule is necessary. |
| `ReLU_Aux_Bound_Examples.active_aux_capture_has_no_linear_certificate` | A mathematical check on the active auxiliary capture | Its linear relaxation has an explicit real model; the sole native nonlinear lemma is essential to the replayed contradiction. |
| `Solver_ReLU_Split_Examples.captured_active_child_unsatisfiable/captured_inactive_child_unsatisfiable` | Native children created by `src/engine/SearchTreeHandler.cpp::performSplit`, traversed by `popSplit`, and written by `JsonWriter::writeUnsatCertificateNode` | Each canonical child is independently checked. The active auxiliary bound is reconstructed using its phase equality, root row, and tableau bound; no native phase bound is silently admitted. |
| `Solver_ReLU_Split_Examples.captured_split_requires_nonlinear_evidence` | The captured `relu_split` query | Its linear relaxation has a proved real model. The observed three native pivots and one split are provenance, not verified C++ state transitions. |

## Deliberate choices

The model permits repeated linear terms, bounds, and ReLU atoms because
conjunction and finite sums give them unambiguous meanings. Splitting filters
out all occurrences of the selected ReLU; each child explicitly implies that
ReLU, so deletion cannot introduce extra parent models. The exact theorem is
`models_relu_split`, with premise `ReLU x y ∈ set (relu_atoms Q)`.

The child-to-parent inclusion holds even without membership
(`satisfies_active_split_iff`, `satisfies_inactive_split_iff`). Membership is
needed for parent-to-children coverage: an unconstrained pair need not satisfy
either ReLU phase.

The original semantic model still permits arbitrary real coefficients. The
executable layer now has separate rational data and a proved normalization into
this semantics. `check_linear_leaf_sound` excludes every real valuation of an
accepted embedded query. We have not silently treated floating-point values as
decimal rationals or replaced HOL real equality with a tolerance test. The
[leaf contract](RATIONAL_LINEAR_CHECKER.md) specifies the representation and
certificate order; the [import contract](CERTIFICATE_IMPORT.md) explicitly
chooses serialized decimal rationals for its processed-query semantics.

## Unresolved semantic questions

1. What is the exact input contract: original decimal/rational network data,
   exact values of finite binary doubles, or a particular serialized query?
   These can denote different real queries.
2. How will preprocessing steps be justified? `Preprocessor::preprocess`
   normalizes inequalities, introduces auxiliaries, tightens bounds, can merge
   weighted layers, and can eliminate variables. The existing proof tree is
   initialized later. A processed-query theorem alone is insufficient.
   [Signed slack introductions](INEQUALITY_AUXILIARY.md) now justify the
   mathematical LE/GE conversion, including finite sequences. Checked finite
   opposite bounds and a local, untrusted translation connect `le`/`ge` files
   to the native file workflow. That route does not execute the native
   preprocessor. The [native preprocessing mode](NATIVE_PREPROCESSING.md) now
   runs `Preprocessor::preprocess` and checks its *result*. Checked slack and
   ReLU introductions are followed by exactly re-derived tightenings and a
   projection onto the captured preprocessed query (or onto crossing bounds
   when preprocessing itself reports infeasibility). The C++ preprocessor
   remains unverified; tolerance-dependent results are rejected.
3. How will checked auxiliary steps cover complete native initialization?
   One scalar-fixed step corresponding to `Engine::addAuxiliaryVariables`
   is now verified in [TABLEAU_AUXILIARY.md](TABLEAU_AUXILIARY.md), including
   an exact connection between explicit source and processed queries for the
   linear capture. [Finite composition](TABLEAU_AUXILIARY_SEQUENCE.md) now
   connects the binary-split capture to an explicit nine-variable source query.
   [Automatic source/step capture and import](SOURCE_QUERY_CAPTURE.md) now cover
   all five tiny native scenarios. One mathematical
   [ReLU auxiliary introduction](RELU_AUXILIARY.md) is now verified too, with
   model extension/projection and checked rational composition.
   [Capture/import of one native call](NATIVE_RELU_INTRO_CAPTURE.md) now also
   checks the whole before/after query and lifts native proof replay to the
   four-variable before-query.
   [Finite ReLU composition and a two-ReLU replay](RELU_AUXILIARY_SEQUENCE.md)
   are now complete too. Native-loop refinement, missing/infinite-bound import,
   and general initialization remain open.
4. How will the running solver emit the supported evidence? The restricted JSON
   writer format is now imported and replayed. An external public-API harness
   has captured actual native linear, ReLU-propagation, and binary-split
   executions, with both split children replayed; the normal CLI still has no
   found writer call site. The configurable Alethe format is not imported.
5. How should support expand beyond the four ReLU upper rules and their exact
   tableau explanations to other propagation rules, missing bounds, and larger trees?
   The current importer explicitly rejects unsupported/missing evidence and
   failed exact reconstruction. It never inherits a C++ success flag as a
   premise; serialized decimal values may still differ from original doubles.
   [Output-lower-to-auxiliary-upper propagation](RELU_OUTPUT_BOUND_PROPAGATION.md)
   now replays the preserved exploratory proof in full. Its dual,
   [positive-auxiliary-lower-to-output-upper-zero](RELU_AUX_LOWER_BOUND_PROPAGATION.md),
   is checked and replayed from a native capture. The remaining native ReLU
   lemma patterns are inventoried in that note.
6. What exact witness reconstruction will suffice for SAT? An approximate
   floating-point assignment is not by itself an exact solution of equalities.
   [Exact SAT assignments](SAT_ASSIGNMENTS.md) now check a rational assignment
   exactly; one native SAT run's exact binary values already form a model.
   A bounded small-denominator repair is attempted otherwise, and failure
   rejects without any SAT or UNSAT claim. Reconstruction remains untrusted.

These questions do not affect the abstract splitting rule or the leaf/tree
soundness theorems. They affect whether the checked rational query and evidence
faithfully represent a concrete Marabou run and its original input.

For future sigmoid support, [ACTIVATION_REUSE.md](ACTIVATION_REUSE.md) records
UAT's existing polymorphic `Sigmoid_Definition.sigmoid` and its real instance.
No duplicate activation definition or additional session dependency is added.
